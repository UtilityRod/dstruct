if(SET)
    message(STATUS "[+] adding Set to dstruct")
    target_sources(dstruct PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/set.c)
    target_include_directories(dstruct PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
endif()